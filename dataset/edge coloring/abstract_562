Relations will be described between the quandle cocycle invariant and the
minimal number of colors used for non-trivial Fox colorings of knots and links.
In particular, a lower bound for the minimal number is given in terms of the
quandle cocycle invariant.