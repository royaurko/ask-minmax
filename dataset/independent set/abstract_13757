We analyze the structure of locally compact groups which can be built up from
p-adic Lie groups, for p in a given set of primes. In particular, we calculate
the scale function and determine tidy subgroups for such groups, and use them
to recover the primes needed to build up the group.