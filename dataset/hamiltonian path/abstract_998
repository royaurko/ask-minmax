We consider the path ideal associated to a line graph, we compute
\texttt{sdepth} for its quotient ring and note that it is equal with its
\texttt{depth}. In particular, it satisfies the Stanley inequality.