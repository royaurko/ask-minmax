We study short intervals which contain an ``almost square'', an integer $n$
that can be factored as $n = ab$ with $a$, $b$ close to $\sqrt{n}$. This is
related to the problem on distribution of $n^2 \alpha \pmod 1$ and the problem
on gaps between sums of two squares.