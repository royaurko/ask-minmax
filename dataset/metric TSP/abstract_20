The algorithm reduces the running time of an algorithm of Frieze from
O(n^{1.5)) to O(n^(4/3 + o)). It also introduces the concept of admissible
permutations that is used in algorithms for obtaining solutions to the AP and
the TSP.